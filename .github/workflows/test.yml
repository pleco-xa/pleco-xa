name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Lint
        run: npm run lint

      - name: Test (vitest)
        run: npm run test

      - name: Build library
        run: npm run build:lib

      - name: Type gate (packed d.ts must type-check for consumers)
        run: npm run check:types

      - name: Agent capability map is current (llms.txt drift gate)
        run: |
          node tools/generate-llms.mjs
          git diff --exit-code llms.txt

      - name: Node import smoke (catches top-level DOM access)
        run: node --input-type=module -e "import('./packages/pleco-xa/dist/pleco-xa.js').then(m => console.log('exports:', Object.keys(m).length))"

      - name: Build demo (consumer e2e)
        run: npm run build -w @pleco-xa/demo

      - name: Build docs site (Starlight + TypeDoc API reference)
        run: npm run build -w @pleco-xa/docs
